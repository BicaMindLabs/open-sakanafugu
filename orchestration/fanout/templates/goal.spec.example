# fanout goal spec example — `fanout goal check <thisfile>` runs the gate to judge whether the goal is met
outcome: add unit tests to module X and make them all green
gate: pytest -q && npm run build
rubric: correctness / no regression / tests truly cover (not an empty run)
rounds: 3
allocate: auto
