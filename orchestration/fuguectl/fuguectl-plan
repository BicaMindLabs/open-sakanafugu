#!/usr/bin/env node
import { runSimpleBridge } from "./fuguectl-node-bridge.mjs";

runSimpleBridge({
  argv: process.argv.slice(2),
  command: "plan",
  helpOnEmpty: false,
  help: `fuguectl-plan "<goal>" [--harness fugue-cc|codex|opencode] [--models m1,m2,..] [--out <dir>]
default models = fugue-cc: cc-deepseek,cc-kimi,coder · codex: gpt-5.5 · opencode: opencode/deepseek-v4-flash-free
default out    = <cache_root>/plans
`,
});
